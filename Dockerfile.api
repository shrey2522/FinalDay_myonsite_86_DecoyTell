FROM python:3.12-slim

WORKDIR /app

COPY . .

RUN pip install --no-cache-dir -r requirements-live.txt -r requirements-web.txt

EXPOSE 8000
CMD ["uvicorn", "decoytell.api:app", "--host", "0.0.0.0", "--port", "8000"]